import React from 'react'
import { container, title } from './style'

const Header = () => {
  return (
    <header css={container}>
      <h1 css={title}>Test Nextjs App</h1>
    </header>
  )
}

export default Header
